class GFG
{
	int isLengthEvenorOdd(Node head1)
	{   int length=0;
	    Node temp=head1;
	    while(temp!=null){
	        length+=1;
	        temp=temp.next;
	    }
	    if(length%2==0){
	        return 0;
	    }
	        else
	        {
	          return 1;  
	        }
	    }
}
